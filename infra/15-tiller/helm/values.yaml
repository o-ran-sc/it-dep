# Default values for tiller-setup.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

images:
 tlsSecrets: ricplt-helm-secrets

ric:
 tiller:
  ricxapp:
   # serviceAccount: tiller
   # secretName: tiller-secret
   # clientSecretName: helm-secret
   # port: 44134
  ricplatform:
   service
 